commit 874b648bf21b4a26d0242055d1698713ffce7169
Author: Vinh Nguyen <vinh.nguyen.df@rvc.renesas.com>
Date:   Fri Sep 16 19:52:54 2016 +0700

    Fix build error duty_ns

    Signed-off-by: Vinh Nguyen <vinh.nguyen.df@rvc.renesas.com>

diff --git a/drivers/pwm/pwm-rcar.c b/drivers/pwm/pwm-rcar.c
index c40e983..ba8f469 100644
--- a/drivers/pwm/pwm-rcar.c
+++ b/drivers/pwm/pwm-rcar.c
@@ -125,22 +125,22 @@ static void rcar_pwm_set_clock_control(struct rcar_pwm_chip *rp,
 static int rcar_pwm_set_counter(struct rcar_pwm_chip *rp, int div, int duty_ns,
				int period)
 {
-    unsigned long long duty_ns, period_ns;
+    unsigned long long duty, period_ns;
	unsigned long long one_cycle, tmp;	/* 0.01 nanoseconds */
	unsigned long clk_rate = clk_get_rate(rp->clk);
	u32 cyc, ph;

    //change period and duty to nano second unit time
    if(rp->time_unit == UNIT_S) {
-       if(period > 264 || duty > 264)
+       if(period > 264 || duty_ns > 264)
            return -ERANGE;
        else {
-           duty_ns = (unsigned long long)duty * 1000000000ULL;
+           duty = (unsigned long long)duty_ns * 1000000000ULL;
            period_ns = (unsigned long long)period * 1000000000ULL;
        }
    }
    else {
-       duty_ns = (unsigned long long)duty;
+       duty = (unsigned long long)duty_ns;
        period_ns = (unsigned long long)period;
    }

@@ -160,9 +160,9 @@ static int rcar_pwm_set_counter(struct rcar_pwm_chip *rp, int div, int duty_ns,
	cyc = (tmp << RCAR_PWMCNT_CYC0_SHIFT) & RCAR_PWMCNT_CYC0_MASK;

	if(rp->time_unit == UNIT_S)
-		tmp = duty_ns;
+		tmp = duty;
	else
-		tmp = duty_ns * 100ULL;
+		tmp = duty * 100ULL;

	do_div(tmp, one_cycle);
	ph = tmp & RCAR_PWMCNT_PH0_MASK;
